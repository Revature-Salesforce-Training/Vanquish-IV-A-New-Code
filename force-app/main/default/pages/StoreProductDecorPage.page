<apex:page controller="DecorController" lightningStylesheets="true">
    <h1>
        Welcome to the Decor Visualforce page!
    </h1>   
    <apex:form >
    <apex:selectList value="{!selectedF}" size="1">
        <apex:selectOption itemValue="name" itemlabel="Name"/>
        <apex:selectOption itemValue="type" itemlabel="Type"/>
        <apex:selectOption itemValue="brand" itemlabel="Brand"/>
        <apex:selectOption itemValue="price" itemlabel="Price"/>
    </apex:selectList>
    <apex:inputText value="{!filterval}">  Filter:  </apex:inputText>        
    <apex:commandButton value="Filter" action="{!filter}" reRender="decorlist"/>
    <apex:commandButton value="Clear Filter" action="{!clear}" reRender="decorlist"/>
    <apex:pageBlock id="decorlist">
        <apex:pageBlockTable value="{!product}" var="decor" rows="5">
            <apex:column value="{!decor.name}"/>
            <apex:column value="{!decor.Type__c}"/>
            <apex:column value="{!decor.Brand__c}"/>
            <apex:column value="{!decor.Price__c}"/>
        </apex:pageBlockTable>  
        <apex:pageblockbuttons >
            <apex:commandButton value="Next Page" action="{!next}" reRender="decorlist"/>
            <apex:commandButton value="Previous Page" action="{!previous}" reRender="decorlist"/>
        </apex:pageblockbuttons>
    </apex:pageBlock>
    </apex:form>
</apex:page>